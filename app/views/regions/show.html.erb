<h2>Welcome to the <%= @region.name %> page!</h2>
<ul>
  <% @users.each do |user| %>
    <li><%= user.email %></li>
  </ul>
<% end %>

<%= form_for current_user do |form| %>
  <%= form.hidden_field :region_id, value: @region.id %>
  <%= form.submit "Join Region!" %>
<% end %>
